{
  "name": "eightbit-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ mager/eightbit-graph",
    "create-local": "graph create --node http://localhost:8020/ mager/eightbit-graph",
    "remove-local": "graph remove --node http://localhost:8020/ mager/eightbit-graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mager/eightbit-graph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
